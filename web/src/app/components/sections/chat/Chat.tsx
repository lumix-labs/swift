"use client";

import React from "react";
import { ChatLayout } from "./ChatLayout";

/**
 * Main Chat component that serves as the entry point for the chat interface
 */
export function Chat() {
  return <ChatLayout />;
}
